Feature: 29 Add new poll (Result Options)
  As a blogger I want to create a poll with results additional options, which then can be added to the page
  User can use additional options while creating poll for results:
  -- sorting of results
  -- setting result bar style (background color, height, border color/height/style(solid/dotted/dashed))
  by clicking "Previous" "Poll Options" tab will be opened (see story #27-Add new poll (Poll Options))
  user does not have "Next" button


  Background:
    Given Add new poll - "Results Options" tab is opened

  Scenario:
    When
    Then
    And
